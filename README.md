# Teste Action Cable
`gem install bundler`
`bundle install`

* Comandos:
`rake db_m:rebuild`
`./sh/limpar.sh`

* Necessário:
Redis
Sidekiq

* Iniciar servidor:
`redis-server`
`bundle exec sidekiq`
`rails s`

